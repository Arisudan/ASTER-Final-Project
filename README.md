# Jarvis

Setup:
1. python -m venv envjarvis
2. envjarvis\Scripts\activate
3. pip install -r requirements.txt
4. Copy `.env.example` to `.env` and set private API keys (do not commit `.env`)

Run:
- python run.py
